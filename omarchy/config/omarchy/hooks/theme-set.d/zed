#!/usr/bin/env bash
set -euo pipefail
source_file="$HOME/.local/state/omarchy/current/theme/zed.json"
[[ -f "$source_file" ]] || exit 0
mkdir -p "$HOME/.config/zed/themes"
cp "$source_file" "$HOME/.config/zed/themes/omarchy.json"
